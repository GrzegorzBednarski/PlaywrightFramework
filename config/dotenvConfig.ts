import { DotenvOptions } from '../utils/dotenv';

export const dotenvConfig: DotenvOptions = {
  enableLogging: true, // Enable configuration loading logs
  override: true, // Override existing environment variables
};
